<UserControl x:Class="ManyWeapons.View.ModelAnimationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ManyWeapons.View"
             FocusManager.IsFocusScope="True"
             KeyboardNavigation.DirectionalNavigation="Cycle"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <!-- Dark theme base colors -->
        <Color x:Key="DarkBackground">#1e1e1e</Color>
        <Color x:Key="DarkPanel">#2d2d30</Color>
        <Color x:Key="AccentOrange">#FF9800</Color>
        <Color x:Key="TextColor">White</Color>

        <SolidColorBrush x:Key="DarkBackgroundBrush" Color="{StaticResource DarkBackground}"/>
        <SolidColorBrush x:Key="DarkPanelBrush" Color="{StaticResource DarkPanel}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentOrange}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>


        <Style x:Key="DarkTabItemContent" TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource DarkPanelBrush}" />
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <!-- Add this -->
            <Setter Property="Margin" Value="0,0,5,5" />
        </Style>

        <Style x:Key="FieldCheckStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,5,5" />
        </Style>

        <!-- Sidebar button style -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabControl dark theme style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource DarkPanelBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="ContentTemplate" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Tab headers -->
                            <TabPanel 
                Grid.Row="0"
                IsItemsHost="True"
                Background="{StaticResource DarkPanelBrush}"
                Margin="0"
                KeyboardNavigation.TabIndex="1" />

                            <!-- Selected tab content (intentionally blank) -->
                            <ContentPresenter 
                Grid.Row="1"
                ContentSource="SelectedContent"
                Margin="0"
                KeyboardNavigation.TabNavigation="Local" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- TabItem style -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Padding" Value="10,2" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource DarkBackgroundBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ThemedSubTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource DarkPanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <TabPanel Background="{StaticResource DarkBackgroundBrush}"
                  IsItemsHost="True"
                  Margin="0"
                  KeyboardNavigation.TabIndex="1"
                  Panel.ZIndex="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>



        <Style x:Key="ThemedSubTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="2">
                            <ContentPresenter ContentSource="Header"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="10,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Selected tab -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DarkBackgroundBrush}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>

                            <!-- Hover (not selected) -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#444444"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- GroupBox style -->
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0.7"/>
        </Style>

        <!-- ComboBoxToggleButton (arrow part) -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border Background="{TemplateBinding Background}" 
        BorderBrush="{TemplateBinding BorderBrush}" 
        BorderThickness="{TemplateBinding BorderThickness}">
                <Grid>
                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                    <Path HorizontalAlignment="Right" VerticalAlignment="Center" 
              Margin="0,0,4,0" 
              Data="M 0 0 L 4 4 L 8 0 Z" 
              Fill="{StaticResource AccentBrush}" />
                    <!--"{TemplateBinding Foreground}"-->
                </Grid>
            </Border>
        </ControlTemplate>

        <!-- ComboBox style -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="HorizontalContentAlignment"
Value="{Binding Path=HorizontalContentAlignment, 
        RelativeSource={RelativeSource AncestorType=ItemsControl, AncestorLevel=1}, 
        FallbackValue=Left}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton"
                              Template="{StaticResource ComboBoxToggleButton}"
                              Grid.Column="2"
                              Focusable="false"
                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                              ClickMode="Press"
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}">
                                <TextBlock Text="{TemplateBinding SelectionBoxItem}"
   Foreground="{StaticResource TextBrush}"
   Margin="4"
   VerticalAlignment="Center"
   HorizontalAlignment="Left"/>
                            </ToggleButton>
                            <Popup Name="Popup"
                       Placement="Bottom"
                       IsOpen="{TemplateBinding IsDropDownOpen}"
                       AllowsTransparency="True"
                       Focusable="False"
                       PopupAnimation="Slide">
                                <Border Background="{StaticResource DarkPanelBrush}"
                            BorderBrush="{StaticResource AccentBrush}"
                            BorderThickness="1"
                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>



        <!-- TextBox style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
        </Style>

        <!-- CheckBox style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Background" Value="{StaticResource DarkBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="18" Height="18"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
                                <Path x:Name="CheckMark"
                  Width="12"
                  Height="12"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center"
                  Visibility="Collapsed"
                  Stroke="{StaticResource AccentBrush}"
                  StrokeThickness="2"
                  Data="M2,6 L5,9 L10,3"/>
                            </Border>
                            <ContentPresenter Margin="5,0,0,0"
                          VerticalAlignment="Center"
                          RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Brushes -->
        <!-- Brushes updated to match orange/black theme -->
        <SolidColorBrush x:Key="arrowButtonForeground" Color="#FF9800"/>
        <!-- Orange -->
        <SolidColorBrush x:Key="StandardBorderBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="StandardBackgroundBrush" Color="#1e1e1e"/>
        <SolidColorBrush x:Key="HoverBorderBrush" Color="#FFB84D"/>
        <SolidColorBrush x:Key="SelectedBackgroundBrush" Color="#2d2d30"/>
        <SolidColorBrush x:Key="SelectedForegroundBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#555"/>
        <SolidColorBrush x:Key="NormalBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="NormalBorderBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="HorizontalNormalBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="HorizontalNormalBorderBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="GlyphBrush" Color="White"/>
        <LinearGradientBrush x:Key="PressedBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFB84D" Offset="0.0"/>
            <GradientStop Color="#FF9800" Offset="0.5"/>
            <GradientStop Color="#FF6F00" Offset="1.0"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="ListBoxBackgroundBrush" StartPoint="0,0" EndPoint="1,0.001">
            <GradientStop Color="#2d2d30" Offset="0.0"/>
            <GradientStop Color="#2d2d30" Offset="0.6"/>
            <GradientStop Color="#1e1e1e" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- ScrollBar Repeat Buttons (at each end) -->
        <Style x:Key="ScrollBarLineButton" TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border
       Name="Border"
       Margin="1"
       CornerRadius="2"
       Background="Transparent"
       BorderBrush="{StaticResource NormalBorderBrush}"
       BorderThickness="0">
                            <Path
           HorizontalAlignment="Center"
           VerticalAlignment="Center"
           Fill="White"
               Data="{Binding Path=Content, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="Border" Property="Background"
               Value="{StaticResource PressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground"
               Value="{StaticResource DisabledForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar Repeat Buttons (middle area) -->
        <Style x:Key="ScrollBarPageButton" TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar Thumb -->
        <Style x:Key="ScrollBarThumb" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border
       CornerRadius="2"
       Background="{TemplateBinding Background}"
       BorderBrush="{TemplateBinding BorderBrush}"
       BorderThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Vertical ScrollBar Template -->
        <ControlTemplate x:Key="VerticalScrollBar" TargetType="ScrollBar">
            <Grid Width="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="18"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="18"/>
                </Grid.RowDefinitions>
                <Border Grid.RowSpan="3" CornerRadius="2" Background="Transparent" />
                <RepeatButton Grid.Row="0" Style="{StaticResource ScrollBarLineButton}" Height="18" Command="ScrollBar.LineUpCommand" Content="M 0 4 L 8 4 L 4 0 Z" />
                <Track Name="PART_Track" Grid.Row="1" IsDirectionReversed="true">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumb}" Margin="1,0,1,0" Background="{StaticResource HorizontalNormalBrush}" BorderBrush="{StaticResource HorizontalNormalBorderBrush}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
                <RepeatButton Grid.Row="2" Style="{StaticResource ScrollBarLineButton}" Height="18" Command="ScrollBar.LineDownCommand" Content="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>

        <!-- Horizontal ScrollBar Template -->
        <ControlTemplate x:Key="HorizontalScrollBar" TargetType="ScrollBar">
            <Grid Height="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="18"/>
                </Grid.ColumnDefinitions>
                <Border Grid.ColumnSpan="3" CornerRadius="2" Background="Transparent" />
                <RepeatButton Grid.Column="0" Style="{StaticResource ScrollBarLineButton}" Width="18" Command="ScrollBar.LineLeftCommand" Content="M 4 0 L 4 8 L 0 4 Z" />
                <Track Name="PART_Track" Grid.Column="1" IsDirectionReversed="False">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumb}" Margin="0,1,0,1" Background="{StaticResource NormalBrush}" BorderBrush="{StaticResource NormalBorderBrush}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageRightCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
                <RepeatButton Grid.Column="2" Style="{StaticResource ScrollBarLineButton}" Width="18" Command="ScrollBar.LineRightCommand" Content="M 0 0 L 4 4 L 0 8 Z"/>
            </Grid>
        </ControlTemplate>

        <!-- Corner Template -->
        <ControlTemplate x:Key="ScrollViewerCorner" TargetType="Control">
            <Border Background="Transparent" />
        </ControlTemplate>

        <!-- Style for overall ScrollBar -->
        <Style TargetType="ScrollBar">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="12"/>
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}"/>
                </Trigger>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="12"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Template" Value="{StaticResource VerticalScrollBar}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for overall ScrollViewer -->
        <Style x:Key="FavsScrollViewer" TargetType="ScrollViewer">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                            <ScrollBar Name="PART_VerticalScrollBar"
           Grid.Column="1" Grid.Row="0"
           Value="{TemplateBinding VerticalOffset}"
           Maximum="{TemplateBinding ScrollableHeight}"
           ViewportSize="{TemplateBinding ViewportHeight}"
           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar Name="PART_HorizontalScrollBar"
           Orientation="Horizontal"
           Grid.Column="0" Grid.Row="1"
           Value="{TemplateBinding HorizontalOffset}"
           Maximum="{TemplateBinding ScrollableWidth}"
           ViewportSize="{TemplateBinding ViewportWidth}"
           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                            <Control Template="{StaticResource ScrollViewerCorner}" Grid.Column="1" Grid.Row="1"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <TabControl Margin="5" Background="{StaticResource DarkPanelBrush}">
            <!-- Models Tab -->
            <TabItem Header="Models">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="5" KeyboardNavigation.DirectionalNavigation="Cycle">

                        <!-- Main Weapon Model Fields -->
                        <GroupBox Header="Weapon Models">
                            <Grid Margin="3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="gunModel" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [gunModel], Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

                                <TextBlock Text="knifeModel" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [knifeModel], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

                                <TextBlock Text="worldModel" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [worldModel], Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>

                                <TextBlock Text="worldKnifeModel" Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [worldKnifeModel], Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>

                                <TextBlock Text="viewHands" Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [handModel], Mode=TwoWay}" Grid.Row="4" Grid.Column="1"/>

                                <TextBlock Text="worldClipModel" Grid.Row="5" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [worldClipModel], Mode=TwoWay}" Grid.Row="5" Grid.Column="1"/>
                            </Grid>
                        </GroupBox>

                        <!-- Separate HideTags Group -->
                        <GroupBox Header="Hidden Tags (one per line)">
                            <TextBox Text="{Binding [hideTags], Mode=TwoWay}"
                         AcceptsReturn="True"
                         Height="100"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         Margin="5"/>
                        </GroupBox>

                        <GroupBox Header="Alternative Models" Margin="10">
                            <Expander Header="Show/Hide Alternative Models" IsExpanded="False" Foreground="White">
                                <Grid>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" Height="400">
                                        <UniformGrid Columns="2" Margin="10 " KeyboardNavigation.DirectionalNavigation="Cycle">
                                            <!-- gunModel2 + worldModel2 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel2" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel2], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel2" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel2], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel3 + worldModel3 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel3" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel3], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel3" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel3], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel4 + worldModel4 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel4" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel4], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel4" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel4], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel5 + worldModel5 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel5" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel5], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel5" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel5], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel6 + worldModel6 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel6" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel6], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel6" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel6], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel7 + worldModel7 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel7" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel7], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel7" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel7], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel8 + worldModel8 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel8" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel8], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel8" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel8], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel9 + worldModel9 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel9" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel9], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel9" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel9], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel10 + worldModel10 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel10" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel10], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel10" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel10], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel11 + worldModel11 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel11" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel11], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel11" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel11], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel12 + worldModel12 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel12" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel12], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel12" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel12], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel13 + worldModel13 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel13" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel13], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel13" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel13], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel14 + worldModel14 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel14" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel14], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel14" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel14], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel15 + worldModel15 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel15" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel15], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel15" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel15], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>

                                            <!-- gunModel16 + worldModel16 -->
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="gunModel16" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [gunModel16], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="worldModel16" Width="100" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding [worldModel16], Mode=TwoWay}" Width="200"/>
                                            </StackPanel>
                                        </UniformGrid>
                                    </ScrollViewer>
                                </Grid>
                            </Expander>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>


            <!-- Anims Tab -->
            <TabItem Header="Anims" Style="{StaticResource ThemedSubTabItem}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="5" KeyboardNavigation.DirectionalNavigation="Cycle">
                        <GroupBox Header="All Animations">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <!-- 29 rows total -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- adsUpAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- adsDownAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- adsFireAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- adsLastShotAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- adsRechamberAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- RechamberAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- idleAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- fireAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- lastShotAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- reloadAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- reloadEmptyAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- reloadStartAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- reloadEndAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- meleeAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- meleeChargeAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- raiseAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- quickRaiseAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- dropAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- quickDropAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- firstRaiseAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- sprintInAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- sprintLoopAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- sprintOutAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- altDropAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- altRaiseAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- deployAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- emptyDropAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- emptyIdleAnim -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- emptyRaiseAnim -->
                                </Grid.RowDefinitions>

                                <!-- Fields -->
                                <TextBlock Text="adsUpAnim" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [adsUpAnim], Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

                                <TextBlock Text="adsDownAnim" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [adsDownAnim], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

                                <TextBlock Text="adsFireAnim" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [adsFireAnim], Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>

                                <TextBlock Text="adsLastShotAnim" Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [adsLastShotAnim], Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>

                                <TextBlock Text="RechamberAnim" Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [rechamberAnim], Mode=TwoWay}" Grid.Row="4" Grid.Column="1"/>

                                <TextBlock Text="adsRechamberAnim" Grid.Row="5" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [adsRechamberAnim], Mode=TwoWay}" Grid.Row="5" Grid.Column="1"/>

                                <TextBlock Text="idleAnim" Grid.Row="6" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [idleAnim], Mode=TwoWay}" Grid.Row="6" Grid.Column="1"/>

                                <TextBlock Text="fireAnim" Grid.Row="7" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [fireAnim], Mode=TwoWay}" Grid.Row="7" Grid.Column="1"/>

                                <TextBlock Text="lastShotAnim" Grid.Row="8" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [lastShotAnim], Mode=TwoWay}" Grid.Row="8" Grid.Column="1"/>

                                <TextBlock Text="reloadAnim" Grid.Row="9" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadAnim], Mode=TwoWay}" Grid.Row="9" Grid.Column="1"/>

                                <TextBlock Text="reloadEmptyAnim" Grid.Row="10" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadEmptyAnim], Mode=TwoWay}" Grid.Row="10" Grid.Column="1"/>

                                <TextBlock Text="reloadStartAnim" Grid.Row="11" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadStartAnim], Mode=TwoWay}" Grid.Row="11" Grid.Column="1"/>

                                <TextBlock Text="reloadEndAnim" Grid.Row="12" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadEndAnim], Mode=TwoWay}" Grid.Row="12" Grid.Column="1"/>

                                <TextBlock Text="meleeAnim" Grid.Row="13" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [meleeAnim], Mode=TwoWay}" Grid.Row="13" Grid.Column="1"/>

                                <TextBlock Text="meleeChargeAnim" Grid.Row="14" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [meleeChargeAnim], Mode=TwoWay}" Grid.Row="14" Grid.Column="1"/>

                                <TextBlock Text="raiseAnim" Grid.Row="15" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [raiseAnim], Mode=TwoWay}" Grid.Row="15" Grid.Column="1"/>

                                <TextBlock Text="quickRaiseAnim" Grid.Row="16" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [quickRaiseAnim], Mode=TwoWay}" Grid.Row="16" Grid.Column="1"/>

                                <TextBlock Text="dropAnim" Grid.Row="17" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [dropAnim], Mode=TwoWay}" Grid.Row="17" Grid.Column="1"/>

                                <TextBlock Text="quickDropAnim" Grid.Row="18" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [quickDropAnim], Mode=TwoWay}" Grid.Row="18" Grid.Column="1"/>

                                <TextBlock Text="firstRaiseAnim" Grid.Row="19" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [firstRaiseAnim], Mode=TwoWay}" Grid.Row="19" Grid.Column="1"/>

                                <TextBlock Text="sprintInAnim" Grid.Row="20" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [sprintInAnim], Mode=TwoWay}" Grid.Row="20" Grid.Column="1"/>

                                <TextBlock Text="sprintLoopAnim" Grid.Row="21" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [sprintLoopAnim], Mode=TwoWay}" Grid.Row="21" Grid.Column="1"/>

                                <TextBlock Text="sprintOutAnim" Grid.Row="22" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [sprintOutAnim], Mode=TwoWay}" Grid.Row="22" Grid.Column="1"/>

                                <TextBlock Text="altDropAnim" Grid.Row="23" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [altDropAnim], Mode=TwoWay}" Grid.Row="23" Grid.Column="1"/>

                                <TextBlock Text="altRaiseAnim" Grid.Row="24" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [altRaiseAnim], Mode=TwoWay}" Grid.Row="24" Grid.Column="1"/>

                                <TextBlock Text="deployAnim" Grid.Row="25" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [deployAnim], Mode=TwoWay}" Grid.Row="25" Grid.Column="1"/>

                                <TextBlock Text="emptyDropAnim" Grid.Row="26" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [emptyDropAnim], Mode=TwoWay}" Grid.Row="26" Grid.Column="1"/>

                                <TextBlock Text="emptyIdleAnim" Grid.Row="27" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [emptyIdleAnim], Mode=TwoWay}" Grid.Row="27" Grid.Column="1"/>

                                <TextBlock Text="emptyRaiseAnim" Grid.Row="28" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [emptyRaiseAnim], Mode=TwoWay}" Grid.Row="28" Grid.Column="1"/>
                            </Grid>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>




            <TabItem Header="State Timers" Style="{StaticResource ThemedSubTabItem}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="5" Orientation="Vertical" KeyboardNavigation.DirectionalNavigation="Cycle">

                        <!-- Group 1: Fire & Melee Timers -->
                        <GroupBox Header="Fire">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="fireTime" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [fireTime], Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

                                <TextBlock Text="fireDelay" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [fireDelay], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

                            </Grid>
                        </GroupBox>


                        <!-- Group: Rechamber Timers -->
                        <GroupBox Header="Rechamber Timers">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="rechamberTime" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [rechamberTime], Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

                                <TextBlock Text="rechamberBoltTime" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [rechamberBoltTime], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>
                            </Grid>
                        </GroupBox>


                        <!-- Group 2: Reload Timers -->
                        <GroupBox Header="Reload Timers">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="reloadTime" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadTime], Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

                                <TextBlock Text="reloadAddTime" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadAddTime], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

                                <TextBlock Text="reloadEmptyTime" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadEmptyTime], Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>

                                

                                <TextBlock Text="reloadStartTime" Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadStartTime], Mode=TwoWay}" Grid.Row="4" Grid.Column="1"/>

                                <TextBlock Text="reloadStartAddTime" Grid.Row="5" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadStartAddTime], Mode=TwoWay}" Grid.Row="5" Grid.Column="1"/>

                                <TextBlock Text="reloadEndTime" Grid.Row="6" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [reloadEndTime], Mode=TwoWay}" Grid.Row="6" Grid.Column="1"/>

                            </Grid>
                        </GroupBox>

                        <!-- Group 4: Sprint & ADS Timers -->
                        <GroupBox Header="Sprints">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                             

                                <TextBlock Text="sprintInTime" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [sprintInTime], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

                                <TextBlock Text="sprintLoopTime" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [sprintLoopTime], Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>

                                <TextBlock Text="sprintOutTime" Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [sprintOutTime], Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>

                               
                            </Grid>
                        </GroupBox>

                        <!-- Group: Melee Timers -->
                        <GroupBox Header="Melee Timers">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="meleeTime" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [meleeTime], Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

                                <TextBlock Text="meleeDelay" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [meleeDelay], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

                                <TextBlock Text="meleeChargeTime" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [meleeChargeTime], Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>
                                <TextBlock Text="meleeChargeDelay" Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [meleeChargeDelay], Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>
                            </Grid>
                        </GroupBox>

                        <!-- Group 3: Raise & Drop Timers -->
                        <GroupBox Header="Raise / Drop Timers">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="raiseTime" Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [raiseTime], Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

                                <TextBlock Text="dropTime" Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [dropTime], Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

                                <TextBlock Text="altRaiseTime" Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [altRaiseTime], Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>

                                <TextBlock Text="altDropTime" Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [altDropTime], Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>

                                <TextBlock Text="quickRaiseTime" Grid.Row="4" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [quickRaiseTime], Mode=TwoWay}" Grid.Row="4" Grid.Column="1"/>

                                <TextBlock Text="quickDropTime" Grid.Row="5" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [quickDropTime], Mode=TwoWay}" Grid.Row="5" Grid.Column="1"/>

                                <TextBlock Text="firstRaiseTime" Grid.Row="6" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [firstRaiseTime], Mode=TwoWay}" Grid.Row="6" Grid.Column="1"/>

                                <TextBlock Text="emptyRaiseTime" Grid.Row="7" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [emptyRaiseTime], Mode=TwoWay}" Grid.Row="7" Grid.Column="1"/>

                                <TextBlock Text="emptyDropTime" Grid.Row="8" Grid.Column="0" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding [emptyDropTime], Mode=TwoWay}" Grid.Row="8" Grid.Column="1"/>
                            </Grid>
                        </GroupBox>

                        

                        
                    </StackPanel>
                </ScrollViewer>
            </TabItem>



        </TabControl>
    </Grid>
</UserControl>
